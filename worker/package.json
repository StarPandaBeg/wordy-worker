{
	"name": "wordy-worker",
	"version": "1.0.0",
	"description": "Cloudflare Worker for Wordy Bot",
	"main": "index.ts",
	"scripts": {
		"dev": "wrangler dev",
		"db:new": "wrangler d1 create wordy-db",
		"db:init": "wrangler d1 execute wordy-db --local --file=./database/schema.sql"
	},
	"author": "StarPanda",
	"license": "MIT",
	"dependencies": {
		"@cfworker/web": "^1.14.0"
	}
}
